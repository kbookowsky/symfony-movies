{% extends "./base.html.twig" %}

{% block body %}
    <div class="w-4/5 m-auto pt-40 pb-10">
        <h1 class="text-6xl text-center pb-12">
            Create Movie
        </h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="uppercase mt-15 bg-blue-900 text-white py-3 px-6 rounded-lg w-fit ml-auto">
            Add Review
        </button>
        {{ form_end(form) }}
    </div>
{% endblock %}